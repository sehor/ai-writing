# AI Writing Studio — 中文桌面工作台

## 使用目标

以长篇正文写作为中心。保留雪花规划、故事设定、记忆与文风、结构分析、正文写作五个领域。AI 提案必须经人工审核；本机暂存不等于正式正文。

## 视觉规范

- 默认温和浅色，可切换深色；颜色由 `frontend/src/styles/tokens.css` 的语义变量控制。
- 界面使用本机中文无衬线字体，14px；正文默认 18px、1.9 倍行高，支持 16/18/20/22px。
- 以分隔线、空白和文字层级组织工作。绿色用于当前选择和主要操作；红色用于错误，警告与状态同时使用文字。
- 正文区域保持单一书写表面，避免堆叠卡片和重复显示同一正文。仅技术详情使用等宽字体。

## 布局与状态

- 桌面：184px 模块导航、216px 章节目录、弹性正文。参考、场景设置、版本、分析与回写按需打开；1600px 以上辅助面板可并排显示。
- 1280px 以下收窄导航；900px 以下目录采用抽屉；600px 以下主导航采用抽屉。
- 正文保存后保留编辑器、光标和滚动位置，更新版本基线。自动暂存采用既有 400ms 延时，明确显示失败。
- Ctrl+S 在正式正文中保存版本，在 AI 草稿中只暂存。输入法组合输入和打开对话框时不触发编辑快捷键。
- 专注模式收起主导航与目录；Escape 依次关闭辅助区域或退出专注。
- 项目导航位置与外观偏好独立保存在 `ai-writing:ui:v1:`，业务草稿缓存格式不变。

## 中文术语

Canon → 故事设定；Memory → 记忆与文风；Manuscript → 正文；Proposal → 待审核草稿／建议；Revision → 版本；Write-back → 变更回写；Scene Contract → 场景契约。

## 验证

`pnpm lint`、`pnpm build`、`pnpm test`。`node tests/workspace-browser.mjs` 使用随机端口和隔离 API 数据，覆盖浅深主题、1440/1280/390px、编辑保存、草稿暂存、重载恢复、专注模式及筛选，并输出 `.tmp/ui-review/` 截图。

`pnpm test:e2e` 使用临时 SQLite 数据库验证本地生成、人工审核、连续保存、导出、版本冲突、刷新恢复及变更回写，并覆盖分析文件写入失败后的重试与版本去重。
