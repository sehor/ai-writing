# 建立长篇项目性能基线和容量边界

审查基线：本地提交 `7147ba58a135fd5a4a258e9ccb5b1d826acdf389`（2026-09-07）。创建 issue 时该提交尚未在 origin 查询到；下列位置为该本地基线的仓库相对路径。请在修复窗口核对当前分支，不要为了匹配旧行号回退他人修改。

**优先级 / 类型**：P3 / enhancement。本任务只负责下述范围；不包含其他 issue 的整片重构。

**问题与证据**

验证缺口：本次未进行长篇规模压力测试；多处首屏全量加载和 snapshot 构建不能仅凭单元测试判断适合长篇规模。这里是测量任务，不先认定存在具体性能故障。

**涉及位置**

- `frontend/src/stores/workspace.ts`
- `frontend/src/stores/manuscript.ts`
- `backend/app/narrative/snapshot.py`
- `backend/app/services/backup_service.py`
- `backend/app/data/repositories/`
- `e2e/`

**修复边界**

用合成作品构建小/中/大规模夹具，测量首屏、切项目/场景、历史加载、snapshot、导出和备份；记录 999 场景等现有硬约束与实际可支持范围。不先引入分页框架或新依赖。

**验收标准**

- [x] 脚本可重复，数据隔离，说明机器、数据规模、运行命令、耗时和资源指标。
- [x] 区分 API/数据库/前端渲染成本，找出实测瓶颈再开具体优化单。
- [x] 验证接近硬上限及超限时的明确错误，无静默截断作者数据。
- [x] 给出依据测量制定的初始预算和容量说明，未测部分明确。

**完成记录（2026-09-07）**

20/200/999场景夹具、每场景2,000字与3份修订的数据库/HTTP/真实浏览器测量均完成，记录机器、5次中位/最大耗时、响应字节、CDP主线程/布局和内存指标；校验全部场景/历史及备份恢复正文SHA-256。序号1000、40,001字HTTP写入明确422且无新增修订。4项轻量unittest通过；`pnpm test:perf` 入口本机通过，CI仅加入20场景门禁，远端未执行。

999档历史面板中位约5秒、最慢20秒，不声明流畅；已建本地PERF-01/02具体后续项，未在此测量任务中引入分页/新依赖。初始交互参考档200场景，更多历史/附件/真实作者/生产包/低配机器未测。一次全档调用触及执行器时限，随后单独大档完整通过，未掩盖该失败记录。

[基线、边界及命令](../../performance-baseline.md)；[固定结构化结果](../../performance/2026-09-07-baseline.json)；[优化后续项](../../performance-followups.md)。

**验证要求**

提供可重复 benchmark/压力脚本及结果；日常 CI 保留轻量代表性规模，重测可单独执行。

执行命令遵循仓库 AGENTS.md：Windows/PowerShell，命令通过 rtk，前端 pnpm，后端 uv。使用临时数据库/合成文本，勿覆盖作者实际数据。

**依赖与协作**

前置任务：AUD-08、AUD-12、AUD-15。

不要同时修改其他窗口正在处理的相同文件；保留他人修改。开始前复核依赖 issue 是否关闭，完成后说明改动、验证、兼容性及剩余限制。

